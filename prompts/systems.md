Use $engineer-production-systems for this authorized low-level systems task. Trace ownership, lifetimes, bounds, integer conversions, parser or protocol state, concurrency, cleanup, privilege boundaries, and failure paths. Prefer memory-safe components for new exposed code where feasible. Use target-specific compiler hardening, sanitizers, fuzzing, minimized reproducers, and regression corpora; never infer memory safety from a regex scan.
